# Good Style Guide Fixture

Use token references such as `theme.colors.textHint` and `theme.colors.borderForm`.

Allowed literals include `#0f1e34`, `#ff9a1f`, and `rgba(74, 96, 144, 0.4)`.

```old
#6effc0
theme.colors.notARealToken
```

`#123456` <!-- style-guide-ignore -->
