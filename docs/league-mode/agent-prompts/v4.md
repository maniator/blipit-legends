# League Mode — v4 Agent Prompt

> Paste this entire file into a `@pm-agent` request to start v4 planning + execution.

## Goal

Deliver the **complete-game** experience: long seasons, awards ceremony, optional carryover and minors, optional shareable seeds + replay.

## Prerequisites

- v3 shipped; bracket + trade engine running through at least one full Standard season post-launch with no determinism regressions.
- Doc-count budget measured on real users running 5+ completed Standard seasons (owner `@rxdb-save-integrity`) — establishes whether `seasonArchives` ships enabled or stays gated.
- Multi-team manager mode UX spec ([`../multi-team-manager.md`](../multi-team-manager.md)) reviewed and signed off by `@ux-design-lead`.

## Required reading (in order)

1. [`../roadmap.md`](../roadmap.md) — v4 ships block + acceptance criteria
2. [`../decisions.md`](../decisions.md) — #20–#26, plus the global "won't ship" register
3. [`../data-model.md`](../data-model.md) — `seasonAwards` (v4), `seasonArchives` (v4 gated), `customTeams.minorLeaguePlayerIds` (v4), feature-flag snapshot
4. [`../awards-and-archive.md`](../awards-and-archive.md) — composite formulas (revised per `@baseball-manager`), MoY `expectedWins` formula, RoY rookie definition, PA/IP floors, archive checksum + corruption behavior
5. [`../playoffs-and-trades.md`](../playoffs-and-trades.md) — unchanged; v4 reads playoff records into awards
6. [`../routing.md`](../routing.md) — `/leagues/:seasonId/awards`, `/leagues/:seasonId/leaders`, `/leagues/:seasonId/history`
7. [`../multi-team-manager.md`](../multi-team-manager.md) — v4 multi-team prompt routing, queueing, opposing-managers rule
8. [`../setup-wizard.md`](../setup-wizard.md) — Full preset surfacing; "Enable minor leagues" Advanced toggle; "Carry over from previous season" toggle on Start New Season; "Sim full season" mode entry point
9. [`../ui-reuse.md`](../ui-reuse.md) — awards + leaders screens are pure reuse of `CareerStats*` tables
10. [`../save-export.md`](../save-export.md) — bundle gains `seasonAwards`; archive policy on import collisions
11. [`../style-guide-additions.md`](../style-guide-additions.md) — confirm any v4 components are signed off

## Locked decisions to enforce in v4

| Topic                   | Value                                                                                                                                                                                                                                                |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Preset                  | Full (24) added                                                                                                                                                                                                                                      |
| Season length           | Marathon (120) added                                                                                                                                                                                                                                 |
| Awards (per league)     | MVP, Cy Young, Reliever of the Year, Rookie of the Year, Manager of the Year (composites with revised weights — see awards doc)                                                                                                                      |
| Pitcher MVP eligibility | Pitchers eligible (unified composite includes pitching value); RoY also pitchers-eligible                                                                                                                                                            |
| Award PA / IP floors    | MVP ≥ 60% of team games OR ≥ 3.1 PA × team games; Cy Young ≥ 1.0 IP × team games (SP) OR ≥ 25 appearances (RP); RoY rookie + 130 AB / 50 IP floor                                                                                                    |
| RoY definition          | Player who has zero `seasonPlayerState` rows in any earlier completed season; (or, if v4 minors enabled, promoted from minors during this season)                                                                                                    |
| MoY `expectedWins`      | Pinned formula `expectedWins = (0.500 + (sigmoid(teamTalentZ − leagueMeanTalent) − 0.5) × k) × seasonGames` with `k = 0.50` (tunable); snapshotted **per-team** to `seasonTeams.expectedWins` at season start (NOT to `seasons` — value is per-team) |
| Award tie-break         | **Player awards** (MVP, Cy Young, Reliever of the Year, Rookie of the Year): composite tie → lex-sorted `playerId` ascending (zero PRNG). **Manager of the Year**: composite tie → lex-sorted `seasonTeamId` ascending (zero PRNG).                  |
| Minor leagues           | Off by default; ~10 generated players per team; auto-promote on injury, manual promote/demote                                                                                                                                                        |
| Carryover               | Off by default; on toggle: heal IL + reset fatigue + keep team rosters and league membership                                                                                                                                                         |
| "Sim full season"       | Per-tick fairness budget (e.g., 4 games per `requestIdleCallback`); progress UI; user can cancel; partial completion preserved (idempotency contract holds)                                                                                          |
| Multi-team manager mode | Off by default; toggle in setup; per-game prompt routing rules per `../multi-team-manager.md`                                                                                                                                                        |
| Season archival         | Only if measured doc count exceeds budget (owner `@rxdb-save-integrity`). When triggered: gzip+base64+FNV-1a checksum on `seasonArchives`                                                                                                            |
| Feature flags           | `featureFlags.shareableSeasonSeeds` and `featureFlags.allowReplay` — both default off; snapshotted on `seasons` at create                                                                                                                            |
| `rulesetVersion`        | Bump to `4` if any v1–v3 constant changed; new v4 phases (awards, minors) are gated by `rulesetVersion >= 4`                                                                                                                                         |

## Non-negotiable contracts

- All v1+v2+v3 contracts hold.
- **Awards are pure functions** of `seasonPlayerState.seasonStats` + `seasonTeams` totals + `seasons` snapshot fields. Same season state → same awards.
- **Award formulas are pinned to `rulesetVersion`.** A v4.1 retune cannot retroactively change a season's stored `seasonAwards.formula` breakdown.
- **`seasonArchives` integrity:** `checksum: string` (FNV-1a of `canonicalJSON(archivedData)` pre-gzip; reuse `src/features/saves/storage/canonicalJSON.ts`) verified on read. Mismatch logs `data-integrity` error and surfaces "archive corrupted — historical detail unavailable" without deleting (manual recovery path preserved). `seasons` and `seasonAwards` stay queryable.
- **Archive write order:** insert `seasonArchives` doc → re-read to confirm → only then delete source docs. RxDB has no multi-collection transactions; this order is the recovery contract.
- **Carryover order of operations:** (1) mark previous season `complete`/`abandoned` (releases lock); (2) apply carryover write-back to `customTeams` via sanctioned-write capability; (3) begin new season setup. Step 1 always precedes step 2.
- **Multi-team manager** lifts `teamIds` from length 1 to length N **without rewriting** the v1 manager-mode prompt surface (the v1 refactor anticipated this).
- **Cancellation of "Sim full season"** keeps every completed game; the season stays in a consistent state. Idempotency contract makes this safe.
- **`activeLeagueIds` cache** stays accurate via subscription to `seasons.$` (per `../data-model.md` lock-cache invalidation rule); v4 carryover is the only sanctioned writer that mutates `customTeams` post-season.

## Agent route

| Slice                                                          | Lead agent                                |
| -------------------------------------------------------------- | ----------------------------------------- |
| Phase plan + risk + archive-enable go/no-go                    | `@pm-agent` → `@senior-lead`              |
| Award composite formula reweights + RP / RoY / MoY definitions | `@baseball-manager`                       |
| Award computation determinism + tie-breakers                   | `@simulation-correctness`                 |
| `seasonArchives` schema + checksum + corruption behavior       | `@rxdb-save-integrity`                    |
| Awards + Leaders screens; multi-team manager UI; setup polish  | `@ux-design-lead` → `@ui-visual-snapshot` |
| "Sim full season" fairness budget + cancellation UX            | `@ux-design-lead` → `@safe-refactor`      |
| E2E coverage (Full preset, awards screen, archive round-trip)  | `@e2e-test-runner`                        |

## `@senior-lead` gates

1. Archive enable/disable decision (driven by measured doc-count from real users).
2. Multi-team manager prompt-routing model (queue vs tabs vs primary-team priority — see `../multi-team-manager.md`).
3. Feature-flag default state (`shareableSeasonSeeds`, `allowReplay`) — must remain off by default.

## Acceptance criteria

- A Full 24-team / 120-game Marathon season completes inside the per-tick budget without UI freeze.
- Awards screen renders the full formula breakdown for every winner ("OPS-proxy: .823 · Team W-L bonus: +12 → composite 0.711, rank 1 of 156").
- Award PA / IP floors reject under-qualified candidates; RoY shows "No eligible rookies this season" empty state when applicable.
- Carryover preserves rosters + clears IL/fatigue + resets `pitcherDaysRest`/`pitcherAvailability`/`wear`; league membership preserved.
- Carryover **never** writes to a locked `customTeams` doc (asserted by unit test).
- Archive round-trip is lossless **and** detects corruption via FNV-1a checksum mismatch (asserted by injecting a single-byte mutation into `compressed`).
- Multi-team manager mode: when N>1 teams have simultaneous decisions on the same game-day, prompts route per `../multi-team-manager.md` rule (queueing or primary-team priority).
- "Sim full season" can be cancelled mid-batch; resuming completes only the unfinished games.
- Feature-flag snapshot: flipping a flag mid-season is a no-op until the next season starts.
- Loading a v1/v2/v3 save in v4 keeps the season on its original `rulesetVersion` — no v4 phases (awards, minors) are forced onto a pre-v4 season.
- **Empirical RxDB collection-cap verification** on Chrome / Safari / Firefox (owner `@rxdb-save-integrity`) — confirms the practical 16-cap guideline before the v4 baseline.
- **Multi-team manager UX detailed visual mockups** + "primary-team auto-resolve" knob signed off by `@ux-design-lead` before v4 implementation PR (stub spec is in [`multi-team-manager.md`](../multi-team-manager.md)).
- **Bulk-delete autogen action** in the Custom Teams page (owner `@safe-refactor`) — manual cleanup acceptable through v3; library bloat from many seasons of autogen teams hits real users at v4 scale.
- **Catcher-wear retune** (owner `@baseball-manager`) using the v3 season log — universal `wear += 1/game` ships through v3 as a known limitation; v4 either retunes or accepts permanently.
- **Awards composite formula re-tuning post-launch** (owner `@baseball-manager`) is itself a `rulesetVersion` bump per the "Awards composite formula tuning post-launch breaks historical award docs" risk; tracked as a v4.x maintenance item, not a v4-launch blocker.

## Risks to watch (from `../risks.md`)

- #1 (collection-count budget at v4), #16 (awards favor wrong winner), #17 (archive corruption), #18 (carryover writes locked doc), #20 (sim-full-season UI freeze), #25 (multi-team-mode breaks v1 surface), #26 (feature-flag mid-season toggle).

## Testing surface (must ship in v4 PR)

- **Unit:** every composite formula handles edge cases (zero PA / IP / appearances, ties); leader-table tie-break by `playerId` for player awards and `seasonTeamId` for MoY; archive round-trip preserves all fields; archive failure aborts cleanly (no partial deletion); checksum mismatch surfaces error without delete; carryover write-back uses sanctioned capability.
- **Integration:** complete Standard season produces a non-empty award doc for every catalog entry; archive + recovery produces identical history-screen output.
- **E2E:** Full Marathon season completes via "Sim full season"; awards screen visual snapshot (mobile + desktop); leaders screen visual snapshot; multi-team manager prompt-routing E2E.

## Out of scope for v4

Anything not on the roadmap v4 list. Salaries / contracts / FA / aging / HoF / cross-season comparisons / real MLB data / networked multiplayer — see the "Won't ship in v1–v4" register in `../decisions.md`.
