#!/usr/bin/env sh
# Reject commits with nothing staged (prevents accidental empty commits).
if git diff --cached --quiet; then
  echo "❌ Empty commit rejected: nothing is staged." >&2
  echo "   Stage your changes before committing, or drop the --allow-empty flag." >&2
  exit 1
fi
yarn lint-staged
