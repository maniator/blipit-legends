# Deferred Work

Goals deferred from League Mode v1 Phase 4 — implement in this order after Spec 1 ships.

## Phase 4 Deferred Goals

2. **Style tokens** — `StatusPill` (Fresh/Tired/Spent), `StatusBanner`, `EmptyState` shared components in `src/shared/components/`
3. **LeaguesHubPage** — 5 canonical states, "Continue: season · day X/30" CTA (first focusable), Quick Start bypass, empty state
4. **LeagueSetupWizard** — 6-step wizard (v1: steps 1,2,3,5,6), `wizardReducer`, sessionStorage persistence with `_v:1` key
5. **Season pages** — `SeasonHomePage` (standings + schedule preview + next-game CTA), `SeasonSchedulePage` (day-by-day), `SeasonTeamPage` (roster + fatigue badges)
6. **GamePage `?seasonGameId=` rehydration** — clean `if (seasonGameId)` fork; exhibition path untouched; result write-back to RxDB on game complete
7. **CustomTeamEditor `mode="locked"`** — locked banner (StatusBanner), `sensors={[]}` DnD, editorReducer guard as no-op, destructive actions hidden
8. **Home-screen Continue + `/teams` Auto badge** — StatusBanner "Continue: season · day X/30" on HomeScreen when active season; `<AutoBadge>` next to autogenerated teams in /teams list
